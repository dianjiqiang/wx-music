<!--components/area-header/area-header.wxml-->
<view class="header">
  <view class="title">
    {{title}}
  </view>
  <view class="more" wx:if="{{isMore}}" bindtap="handleClickMore">
    {{more}}
    <van-icon name="arrow" />
  </view>
</view>
